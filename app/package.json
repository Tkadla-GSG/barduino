{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && yarn build:clean",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "build:clean": "cd build && mkdir dist && cp static/js/*.js dist/main.js && cp static/js/*.map dist/main.js.map"
  }
}